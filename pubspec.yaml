name: snapal
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  # Assets (Görseller)
  assets:
    - assets/images/
    - assets/icons/
    
  # Alternatif: Tek tek dosya belirtme
  # assets:
  #   - assets/images/logo.png
  #   - assets/images/logo_white.png
  #   - assets/images/logo_mini.png
  #   - assets/icons/app_icon.png